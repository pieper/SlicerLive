Okay, now I'm going to want you to put together several of the threads from the projects I have loaded in this workspace.  In the end I want the SlicerLive gallery and any of the scenes to be rendered remotely on a modal.com instance and streamed to a browser page locally using the nvenc, QUIC, and WebCodecs path we have developed for desktopia.  I want the backend container to have a very thin set of dependencies.  We have built and debugged this container for the SlicerLive/Renderer example that currently up on modal.com rendering the bumblebee microCT scan (I don't recall which directory that's in, so check your chat histories to find the place we discussed it so we don't need to reinvent all that).  The backend server running on modal should use the rust WebGPU implementation on top of Vulkan using the wgpu-py package to manage the rendering, and it should use the volume rendering infrastructure from SlicerWGPU.  Please research this first and present me with a specific plan so I can be sure you understood the instructions.  Research in the loaded directories, use the skill, and search chat histories for related threads.
